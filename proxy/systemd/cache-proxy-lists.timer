[Unit]
Description=Daily refresh of the cache proxy block lists

[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
# Run a missed refresh at next boot (e.g. the machine was off overnight).
Persistent=true

[Install]
WantedBy=timers.target
