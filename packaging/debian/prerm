#!/bin/bash
set -e

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  systemctl stop cache-proxy-lists.timer cache-proxy-lists-hourly.timer cache-proxy-vendor-cas.timer cache-blockpage.service cache-proxy.service cache-webui.service 2>/dev/null || true
fi

exit 0
