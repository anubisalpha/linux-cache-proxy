# Regular expressions (Python syntax, case-insensitive) searched in the full
# URL, e.g. https://host/path?query. A match blocks the request. One per line,
# "#" for comments. Only applies to traffic the proxy decrypts. Invalid lines
# are skipped with a warning in the log.
#
# \.(torrent)(\?|$)
# /proxy-bypass/
