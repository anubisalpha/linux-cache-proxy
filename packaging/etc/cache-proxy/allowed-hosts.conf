# Hosts that content filtering never blocks, even if they (or a parent
# domain) appear in blocked-hosts.conf, or match blocked-url-patterns.conf.
# Use it to fix false positives from a large imported blocklist.
# Same format as blocked-hosts.conf.
#
# docs.example.com
