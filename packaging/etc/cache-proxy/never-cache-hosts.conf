# Hosts that are proxied and TLS-intercepted normally, but never written
# to the cache -- one hostname pattern per line, "#" for comments.
# Matches the bare host and any subdomain: "example.com" also matches
# "download.example.com". A "*." prefix is accepted but not required.
#
# internal-crm.example.com
