# Extra [tls_trust] seed hosts, added from the web UI's Certificates page.
# One hostname per line -- additive to config.toml's own seed_hosts list,
# not a replacement for it. Edited by hand is fine too; picked up within
# seconds, same as the other host-list files (see
# config.vendor_ca_seed_hosts()).
